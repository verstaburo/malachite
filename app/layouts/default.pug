include _externalIncludes
include _internalIncludes

doctype html
html(lang='ru')
  include head
  body
    block header
    - var isLink = (logoLink || false)
    +header(isLink)
    +b.MAIN.page
      block content
    +footer
    +sticky-navigation.js-fancybox(data-src="#navigation")
    +popups
      +main-navigation#navigation
      block popup
    script(src='assets/scripts/app.min.js')
    script(src='assets/scripts/main.js')
